"""geracao_visual/imagem/ — ESTÁTICO/CARROSSEL renderizados 100% por motor
de template (HTML/CSS via Chromium headless), sem IA generativa.

decisoes-de-engenharia.md, seção 2 (Template Method): o texto do slide
entra como texto real no HTML — nunca desenhado por um modelo de imagem —
então nunca sai cortado nem corrompido. Cores/tipografia/cabeçalho/rodapé
vêm dos design tokens de perfil-marca.md (`identidade_visual`).
"""
